server:
  port: 8080
spring:
  datasource:
    driver-class-name: org.h2.Driver
  jpa:
    database-platform: org.hibernate.spatial.dialect.h2geodb.GeoDBDialect
    open-in-view: false
    hibernate:
      ddl-auto: update
      show-sql: true

jwt:
  secret: ${JWT_SECRET}